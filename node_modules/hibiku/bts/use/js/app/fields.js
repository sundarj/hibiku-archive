define(function(require) {
    var nest = require('./nest');
    var MediumEditor = require('../lib/editor');
    console.log(nest.qs('.hb-file'));
    
    var hb = hb || {
        config: {
            editor: '.hb-editor',
            form: '.hb-editor-form'
        },
        editor: {
            init: function (sel) {
                return new MediumEditor(sel, {
                    disablePlaceholders: true,
                    buttons: ['bold', 'italic', 'underline', 'quote', 'image', 'orderedlist', 'unorderedlist', 'header1', 'header2', 'pre'],
                    buttonLabels: 'fontawesome',
                    autoLink: true
                });
            }
        },
    };
    
    hb.editor.form = nest.qs(hb.config.form);
    
    console.log(hb);
});