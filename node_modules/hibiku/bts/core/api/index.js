const apiGet = require('./io/get');
//const apiPost = require('./posts');

function router() {
    return function (app, route) {
        app.use(route.post('/bts/api/', apiGet));
        //app.use(route.post('/bts/api/post/', apiPost));
    };
};

module.exports = {
    use: router()
};
