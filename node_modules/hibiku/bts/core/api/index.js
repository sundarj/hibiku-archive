
const apiGet = require('./io/get');
const apiPost = require('./posts');
const jsonParser = require('body-parser').json();

function router(app) {
    app.post('/bts/api/', jsonParser, apiGet);
};

module.exports = {
    use: router
};
